name = "scc25"

# Set your Cloudflare account ID (run: `wrangler whoami` to see it)
account_id = "0d4fe9a39589a54a3c78bc9455f8ce83"

# Workers runtime compatibility date
compatibility_date = "2025-09-26"

# Optional: enable basic observability
[observability]
enabled = true

# For Pages Functions, Wrangler auto-detects the `functions/` directory if present.
# No build step is required for this repo right now.

